import bisect

class Solution:
    def searchRange(self, nums: list[int], target: int) -> list[int]:
        # Find insertion point from the left
        left_idx = bisect.bisect_left(nums, target)
        
        # If target isn't present, left_idx will be out of bounds or point to a different number
        if left_idx == len(nums) or nums[left_idx] != target:
            return [-1, -1]
            
        # Find insertion point from the right
        right_idx = bisect.bisect_right(nums, target) - 1
        
        return [left_idx, right_idx]
